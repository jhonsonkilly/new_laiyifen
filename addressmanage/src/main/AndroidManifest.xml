<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ody.p2p.addressmanage">

    <application
        android:allowBackup="true"
        android:supportsRtl="true">
        <activity android:name=".selectaddressactivity.SelectAddressAct" />
        <activity android:name=".editaddress.EditAddressActivity" />
        <activity android:name=".receiver.ReceiveAddressActivity" />

        <!-- <activity android:name=".TestActivity"> -->
        <!-- </activity> -->
        <activity
            android:name=".city.CityListActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".location.ProvinceListActivity" />
    </application>

</manifest>